# 🏗️ tg-monitor 智能体架构演进史 (Architecture Evolution)

本文档系统性总结了 tg-monitor 在“多模态文件处理”这一核心痛点上的架构演进，以及在与开发者（USER）高频探讨中诞生的核心设计思想。

---

## 🛑 传统 Bot 的痛点与死局
在传统的 Telegram 机器人开发中，处理文件（图片、视频、PDF等）通常依赖于硬编码（Hardcoding）：
- `if message.text == '压缩图片': run_compress()`
- `elif message.text == '转GIF': run_gif()`

**问题暴露**：
用户的需求是无穷尽且表达随意的（“帮我压一下”、“让这张图小一点”、“拼成长图”），用 `if-else` 永远无法覆盖所有场景，最终会导致代码膨胀为不可维护的“屎山”。

---

## 💡 核心演进：四大 Agentic 架构模式

为了破局，我们抛弃了传统的“代码写死业务”模式，全面转向 **Agentic 智能体驱动架构**。以下是四大核心设计思想（基于开发过程中的思维碰撞得出）：

### 1. I/O 桥接与 Agentic Tooling (智能工具链调度)
- **理念**：“把复杂性交给大模型，让 Bot 保持极简中转。”
- **实现**：
  - Python Bot 层完全剥离了具体的业务处理逻辑。它仅作为“网关”，负责把 Telegram 下发的文件保存到标准的 `Workspace IN (/tmp/tg_files/in/...)`。
  - 通过预设 `config/TOOLCHAIN.md`（定义了服务器上现有的能力如 ffmpeg, ImageMagick, Pandoc），让 AGY 智能体自行阅读并决定如何调用终端工具处理文件。
  - 处理完毕后，Bot 监听 `Workspace OUT` 目录，将产物回传并销毁工作区。

### 2. Recipe Book (声明式标准作业程序)
- **理念**：“无论用户是说‘压缩’还是‘让图小一点’，底层的执行路径必须是确定、唯一的。”
- **实现**：
  - 建立 `config/file_recipes/` 目录。
  - 针对高频的通用场景（图片拼接、视频转GIF、无损压缩），将其抽象为标准的 Markdown 处理手册。
  - AGY 在规划时强制查阅这些菜谱，确保了智能体的灵活性与系统执行的确定性完美结合。并自带**自学习演进机制**（遇到新场景成功解决后自动沉淀为新 Recipe）。

### 3. Router Agent 模式 (意图极速路由)
- **痛点发现**：当用户上传图片并附带文字时，大模型经常产生“模态错位”——到底是要对图片进行物理裁剪，还是要进行视觉问答（QA）？如果混在一个 Prompt 里，模型极其容易精神分裂。
- **理念**：“专业的事交给专业的 Agent 做，先定性，再定量。”
- **实现**：
  - 引入极速分类路由：当收到带附言的文件时，首先启动一个 `gemini-3.6-flash-high` 轻量模型，它只回答一个布尔值：`True`（这是物理转换任务）或 `False`（这是视觉问答任务）。
  - 根据判定结果进行物理隔离分流，彻底解决了大模型因为多重身份导致的智商下降问题。

### 4. Planner-Executor 隔离机制 (盲盒黑盒规划)
- **痛点发现**：多模态大模型“看到”图片的第一本能永远是去解释和描述。如果在文件处理任务中给它传入实体图片，它经常会抛弃动作指令，转而强行向用户描述图片内容，甚至混淆上下文。
- **理念**：“既然你一看图就走神，那干活的时候我就不给你看图。”
- **实现**：
  - 彻底将**“规划（Planner）”**与**“执行（Executor）”**解耦。
  - **Planner (AGY)**：不再接收实体文件。我们只把文件的元数据（绝对路径、大小、后缀）和用户的诉求（caption）发给它。强制它只能查阅文档，并输出纯粹的 JSON 数组包裹的 Bash 命令集合（`<json>["cmd1", "cmd2"]</json>`）。
  - **Executor (Python Bot)**：正则提取 JSON 后，直接在服务器宿主机环境利用 `subprocess` 循环无情执行，并回收产物。
  - **成效**：彻底杜绝了模型在处理任务时的“话痨”和“幻觉”，将稳定性推向了接近原生软件的 100%。

---

## 🎯 总结
这套架构将 Telegram 机器人从“僵化的代码机器”升维成了**“能听懂人话、会查阅说明书、甚至会自动写 Bash 脚本干活的赛博流水线工人”**。
Bot 代码层达到了极致的轻量与克制，而处理能力的上限则随着服务器所安装的工具链 (`TOOLCHAIN.md`) 和大模型能力的提升而无限拔高。
